# Tennis Data Sync Artifacts

Files here are regenerated directly from `the_creation_2` repo, branch `arena/01a015bb-the-creation-2`, the confirmed single source of truth for tennis data.

## Current: tennis-data-regenerated-YYYYMMDD-from-arena-<commit>.json

Structurally matches the app's expected embedded format (`EDITIONS` array, same field names: `n`/`tour`/`tier`/`y`/`dr`/`mc`/`st`/`src`/`m`/`fr`/`rc`/`rounds`). Generated by grouping the arena branch's `master_store_tennis_SSoT.json` by (tournament, tour, year), pulling draw size/status/source from `MANIFEST.json`, and deriving `fr`/`rc`/`rounds` from each edition's actual present round labels — not assumed from tier name.

**Replaces the app's current embedded tennis-data.js**, which is stale: 119 editions / 8,555 matches, missing the ATP/WTA naming reconciliation (Indian Wells/Miami/Madrid/Rome/Cincinnati/Canada) and missing Ningbo entirely.

This artifact: 204 editions / 14,946 matches, naming fix applied, Ningbo present, plus WTA1000 depth (Doha, Guadalajara, Beijing, Wuhan) added by concurrent work on the arena branch.

Spot-checked against known facts before push: Cincinnati 2021 WTA (56-draw) correctly reads R64/6 rounds; Cincinnati 2025 WTA (96-draw) correctly reads R128/7; Ningbo 2024/2025 both show 27 matches; Montreal ATP rows present under the corrected name (not "Canada Masters").

To use: replace the `<script id="tennis-data-json" type="application/json">...</script>` block's contents in the app with this file's contents, then rebundle.

Source commit: arena/01a015bb-the-creation-2 @ 1a3a6f7
